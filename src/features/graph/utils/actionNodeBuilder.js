// Deprecated: this used to build the transient action/content nodes that
// spawned into the graph on click. That behavior was removed in favor of
// a side panel (see NodePanel.jsx) that shows a clicked skill's resources,
// artifacts, and notes without cluttering the graph. Nothing imports this
// file anymore — kept only as a paper trail; safe to delete.
